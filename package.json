{
  "name": "dental-backend",
  "version": "2.0.0",
  "private": true,
  "description": "Cloud-native appointment API built for AWS Lambda and DynamoDB",
  "scripts": {
    "test": "node --test",
    "start": "node local.js"
  },
  "engines": {
    "node": ">=22"
  }
}
